/**
 * 组件配置文件
 * @type {{emapAdvancedQuery: {initFun: string}}}
 */

let config = {
    "emapAdvancedQuery": {
        "initFun": "emapAdvancedQuery"
    }
};

module.exports.config = function(_html) {
    return config;
};
